# ----------------------------------------------------
# Global ignores (applies to both client and server)
# ----------------------------------------------------

# Node modules
node_modules/

# Environment variables
.env
.env.local
.env.*.local
.DS_Store # macOS specific
.vscode/ # VS Code settings (optional, but often ignored)
.idea/ # IntelliJ/WebStorm specific (optional)

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# OS generated files
.DS_Store
.AppleDouble
.LSOverride
# Thumbnails
._*
.Trashes
# Windows
Thumbs.db
ehthumbs.db
System Volume Information/

# ----------------------------------------------------
# Frontend (React with TypeScript) Specific
# (typically in client/ directory)
# ----------------------------------------------------

# Compiled output
build/
dist/

# Dependency caches
.parcel-cache/ # If using Parcel
.next/ # If using Next.js (though not your choice, good to know)

# Editor and build tool specific
.eslintcache
.cache/
coverage/

# ----------------------------------------------------
# Backend (Node.js/Express with TypeScript & Prisma) Specific
# (typically in server/ directory)
# ----------------------------------------------------

# TypeScript compiled output
dist/
*.tsbuildinfo

# Prisma generated client files (already covered by node_modules generally)
# If you explicitly want to exclude generated client in `node_modules/.prisma/client`
# you generally don't need to add it here, as node_modules is ignored.
# However, if you ever change Prisma's output directory, you'd add it here.
# For standard setup, `node_modules/` covers it.

# Database specific files (e.g., SQLite files in development)
*.sqlite
*.sqlite-journal
db.sqlite
test.db

# Additional backend-specific temporary/log files
*.pid
*.seed
*.log
npm-debug.log* # Redundant if already at global level, but safe to include

# Blackbox
.blackboxrules

generated/
